<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    body { font-family: Helvetica, Arial, sans-serif; font-size: 12px; color: #1f2937; }
    h2 { margin: 0 0 8px; color: #0b3a3d; }
    .meta { margin: 0 0 14px; color: #6b7280; }
    table { width: 100%; border-collapse: collapse; }
    th, td { border: 1px solid #d1d5db; padding: 6px 8px; text-align: left; }
    th { background: #eef6f4; }
  </style>
</head>
<body>
  <h2>Admin Training Programs Report</h2>
  <p class="meta">Generated on {{ "now"|date("Y-m-d H:i") }}</p>
  <table>
    <thead>
      <tr><th>ID</th><th>User</th><th>Name</th><th>Muscle Group</th><th>Difficulty</th><th>Duration</th><th>Updated</th></tr>
    </thead>
    <tbody>
      {% for p in programs %}
        <tr>
          <td>#{{ p.id }}</td>
          <td>{{ p.user ? p.user.email : '-' }}</td>
          <td>{{ p.name }}</td>
          <td>{{ p.muscleGroup }}</td>
          <td>{{ p.difficulty }}</td>
          <td>{{ p.duration ?: 0 }} min</td>
          <td>{{ p.updatedAt ? p.updatedAt|date('Y-m-d H:i') : '-' }}</td>
        </tr>
      {% else %}
        <tr><td colspan="7">No rows</td></tr>
      {% endfor %}
    </tbody>
  </table>
</body>
</html>

